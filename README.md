# answercrypt-server

Store server optionally used by **answercrypt** to store the encrypted payload on your own remote server, adding an extra question/answer protection layer with rate-limited attempts.

Default attempt limit: 10.